<UserControl x:Class="EasyPaint.View.ResultPopup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"  
             mc:Ignorable="d"
             
             FontFamily="{StaticResource PhoneFontFamilyNormal}"
             FontSize="{StaticResource PhoneFontSizeNormal}"
             Foreground="{StaticResource PhoneForegroundBrush}"
             Loaded="UserControl_Loaded"
             d:DesignHeight="400" d:DesignWidth="400">


    <Grid Name="mainGrid" RenderTransformOrigin="0.5,0.5" >
        
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF5E6302" Offset="0"/>
                <GradientStop Color="#FF27290E" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <!--
        <Grid.Background>
            <ImageBrush ImageSource="/EasyPaint;component/Assets/lres/bkg/groups_bkg.jpg" Opacity="1"/>
        </Grid.Background>
        -->

        <Grid.RenderTransform>
            <RotateTransform Angle="{Binding PageOrientation, Converter={StaticResource orientationConverter}}" />
        </Grid.RenderTransform>

        <Grid.RowDefinitions>
            <RowDefinition Height="6*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock x:Name="TextBlockResult" 
                       FontFamily="..\Fonts\CURLZ___.TTF#Curlz MT" 
                       Foreground="Lime" 
                       Text="100%" 
                       FontSize="120" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       Opacity="1"/>
        </Grid>

        <ProgressBar x:Name="MyProgressBarResult"  Grid.Row="1" Value="10"></ProgressBar>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Center" Visibility="Visible">
            <Button x:Name="ButtonRedo" BorderBrush="Transparent" Width="100" Height="100" Padding="0" Tap="ButtonRedo_Tap_1">
                <Button.Content>
                    <Image Source="/Assets/buttons/btn_refresh.png"></Image>
                </Button.Content>
            </Button>
            <Button x:Name="ButtonNext"  BorderBrush="Transparent" Width="100" Height="100"  Padding="0" Tap="ButtonAhead_Tap_1">
                <Button.Content>
                    <Image Source="/Assets/buttons/btn_avanti.png"></Image>
                </Button.Content>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>